export interface RouteMilestone {
    name: string;
    path: string;
    color?: string;
}

export const routeMilestones: RouteMilestone[] = [
    {
        name: "draw.io",
        path: "/drawio",
        color: "orange",
    },
];
